// 决策树规则、利率/资产负债表阈值
export default {
  // 利率判定：单次调整幅度(绝对值) >= 50bp 视为应对式加息/应对式降息（触发利率锁→强制防守），方向不限。
  // 货币维度方向规则（非锁）：任何加息→收紧（利空，资金成本升高）；降息/暂停→宽松。
  RATE_REACTIVE_ADJUSTMENT_BP: 50,

  // 零利率区间上限：利率目标上限降至此值(含)及以下视为"降到底"，是应对式调整锁/萨姆锁的解锁条件之一
  ZERO_RATE_FLOOR_PCT: 0.25,

  // 资产负债表变化阈值：变化幅度 < 0.25% 视为"暂停"（不收缩也不扩张）
  BALANCE_SHEET_PAUSE_THRESHOLD_PCT: 0.25,

  // FRED 回溯窗口
  RATE_LOOKBACK_DAYS: 100,
  BALANCE_SHEET_LOOKBACK_DAYS: 90,

  // FRED 系列 ID
  FRED_SERIES: {
    RATE: 'DFEDTARU',
    BALANCE_SHEET: 'WALCL',
    CORE_PCE: 'PCEPILFE',
    TRIMMED_MEAN_PCE_1M: 'PCETRIM1M158SFRBDAL',
    TRIMMED_MEAN_PCE: 'PCETRIM6M680SFRBDAL',
    TRIMMED_MEAN_PCE_12M: 'PCETRIM12M159SFRBDAL',
    UNEMPLOYMENT: 'UNRATE',
    SAHM: 'SAHMREALTIME',           // 萨姆规则实时值（圣路易斯联储官方计算）
    FISCAL_OUTLAYS: 'MTSO133FMS',   // 联邦月度支出（百万美元）——"大市场小政府"原则的直接度量：
                                    // 支出=政府规模；赤字混入收入端，减税型赤字会被误判收紧（2017实证）
    PCE_PRICE_INDEX: 'PCEPI',       // PCE价格指数（月度，含能源食品）——财政支出通胀平减用，
                                    // 剔除通胀后的"实际支出"同比才反映政府真实扩张（名义支出自然增速≈5%会预挂收紧）
    EPU_TRADE: 'EPUTRADE',          // 贸易政策不确定性指数（月度，贸易专项，结构性）
    EPU_DAILY: 'USEPUINDXD',        // 经济政策不确定性指数（日频，新闻编制，时效性——政策转向数天内可见）
    OIL_WTI: 'DCOILWTICO',          // WTI原油现货价（日频）——战争/地缘冲击的市场实时定价代理
    SEMI_IP: 'IPG3344S',            // 半导体及电子元件工业产出指数（月度）
    CREDIT_SPREAD: 'BAA10Y',        // Baa公司债−10年国债信用利差（日频）——参考指标(不参与判定，同失业率/PCE)：
                                    // 信用市场恐惧度，危机前瞻性强(2007-06先于联储降息走阔)。评估证明做锁会误锁复苏期，故仅展示
    YIELD_CURVE: 'T10Y3M',          // 10年−3个月期限利差（日频）——最经受检验的衰退领先指标(领先12-18个月,
                                    // 1968年以来零漏报)。角色(2026-07-17)：参考展示 + 倒挂持续≥3个月时否决进攻档准入
                                    // （只限制最激进档位，不做锁不触发防守——吸取信用利差锁误锁复苏期的教训）
  },

  // 收益率曲线倒挂确认期：连续倒挂交易日数 ≥ 此值（≈3个月）才否决进攻档准入。
  // 只做进攻否决器：历史回放中进攻档几乎不出现（AI维缺历史数据），此规则不影响防守端回测结论
  YIELD_CURVE_INVERSION_CONFIRM_DAYS: 63,

  // 萨姆规则：值 >= 阈值 视为经济进入衰退初期，触发衰退防守锁
  SAHM_TRIGGER_THRESHOLD: 0.5,

  // 锁最短锁存期（2026-07-17 变体评估采纳，V3）：锁触发后至少锁存此天数（≈2个月，
  // 月度回测口径为2个月）才允许"小幅调整解锁"；零利率解锁不受限。
  // 回测：拦住2007-10"-50bp锁定后次月-25bp跟进降息立即解锁"（顶部区域满仓），
  // 年化11.3→11.5%、回撤-17.7→-16.2%、2008覆盖率66.7→77.8%，复苏入场/假阳性不变——纯改善
  LOCK_MIN_AGE_DAYS: 60,

  // 最终档位降档迟滞（2026-07-17 变体评估采纳，V4）：升档（更防守方向，含锁强制）即时生效；
  // 降档（更宽松方向）需决策树连续给出更宽松档满此天数（≈1个月确认，月度回测口径为连续2个月采样）
  // 才生效，期间沿用上一档。回测：唯一拦住2019-12"新冠前2个月退出防守"的变体，
  // 假阳性20/26→17/20、2020覆盖率100%；代价：复苏期恢复各晚约1个月。
  // 与V3组合=当前基线：年化11.7%、回撤-16.2%、2008少亏58.1pp/覆盖94.4%。
  // 诚实披露：组合超额全部来自2000-2009两次大熊，2010起子样本反而-1.0pp/年
  FINAL_DOWNGRADE_CONFIRM_DAYS: 30,

  // 【已评估未采纳】信用利差锁（第3把锁候选，2026-07-16）：曾用 BAA10Y（Baa公司债−10年国债）
  // 做危机前瞻确认器，规则"10年百分位>80且90日走阔>50bp→强制防守"。回测结论：能把2000互联网
  // 泡沫从"滞后68天"变"提前7天"、最大回撤-18%→-15%，但年化11.3%→9.2%——信用利差在复苏期
  // (2009/2012/2020)高位回落慢导致误锁闷杀反弹。用户拍板不采纳(保年化收益)。
  // 通病提示：任何"信用利差高位=防守"规则都有此复苏期误锁问题；ICE HY OAS 在本FRED环境仅3年历史。

  // 财政信号：滚动12月(TTM)联邦支出的**实际(剔除通胀)同比**，变化超过阈值(%)才判定方向
  // 政策原则"大市场小政府"：实际支出扩大 > 阈值 → tight（政府真实变大），收缩 > 阈值 → loose
  // 2026-07-16改用实际口径：名义支出自然增速≈名义GDP≈5%卡在阈值上，让财政约半数月份预挂收紧
  // （假警报主因）。剔除通胀后实际同比中位数≈2.5%，±5%阈值下收紧占比从~50%降到~24%，
  // 只在实际支出激进扩张(COVID级+40%)或真实收缩时触发；回测：防守月114→92、年化/回撤/六危机捕获不变
  FISCAL_TTM_CHANGE_THRESHOLD_PCT: 5,
  FISCAL_LOOKBACK_DAYS: 800, // 需要约25个月观测（12+12+缓冲）

  // 行政信号：双代理一致才定档（与AI供需同模式）——
  // 月度 EPUTRADE（贸易专项）与 日频EPU 7日均线（全政策、日级响应）各取近10年百分位
  EPU_PERCENTILE_TIGHT: 80,  // >80 分位 → tight（政策高压）
  EPU_PERCENTILE_LOOSE: 50,  // <50 分位 → loose（政策环境平静）
  EPU_LOOKBACK_DAYS: 3660,   // 10年
  EPU_DAILY_MA_DAYS: 7,      // 日频指数噪声大，取7日均线再算百分位

  // 行政信号·油价事件层（优先于EPU百分位）：WTI 30天涨跌幅是战争新闻的市场实时定价，
  // 开战/冲突升级当天油价即跳涨，停战/和谈喊话当天即跳水——精确到日，不等指数编制
  // +20%以上 → 战争/供给冲击 → 行政立即收紧（经OR强制防守）；
  // -20%以上 → 战争结束/对抗降级 → 行政立即宽松（若其余三维宽松且无锁，进攻随即成立）
  OIL_SHOCK_WINDOW_DAYS: 30,
  OIL_SHOCK_PCT: 20,
  OIL_LOOKBACK_DAYS: 60,     // 拉60天日线保证能找到30天前的交易日观测
  // 油价水平护栏（2026-07-19 O1采纳，日度重放归因）：飙升判"战争冲击"须同时满足油价处高位
  // （高于近2年中位数）。危机刚过的低位反弹（2009-03 WTI$45、2020-06 $38）EPU必然还在高位，
  // 旧规则误判战争冲击踏空V型底（2009一段-17.5pp）；俄乌型高位飙升（2022-03 $110）不受影响。
  // 评估：日度年化11.31→12.13%、假阳性12→10段、六危机时点逐日不变、四种"低位"定义结果重合（非过拟合）
  OIL_LEVEL_LOOKBACK_DAYS: 730,

  // AI供需信号（长线主线，2026-07-16重构）：纯现金流三件套一致性判定——
  // 沿资金流向：模型调用量趋势(需求侧,最前瞻) → 云厂商capex同比(投资侧) → 半导体产出同比(供给侧,末端)。
  // 移除原SMH-SPY相对收益（股价是供需传导的结果非供需本身，且顺周期泡沫顶部误投宽松票）。
  // 供不应求(三件套向上)=宽松；供过于求(向下)=收紧。三者共识判定(全非空时多数决/一致；缺失降级)。
  // 调用量阈值随窗口重标定（2026-07-17，窗口改为28日均vs前28日均，中心相距28天）：
  // 旧±10%对3周窗≈要求年化4-8倍增速，AI需求增速正常化到健康的+50%/年后 usage 永远
  // 给不出宽松票→进攻档结构性不可达。新阈值：+3%/28天≈年化+47%（健康AI增长可达宽松）；
  // -3%/28天≈年化-32%（月度均值持续下滑才算真实需求转弱，摊薄节假日单周噪声）。
  // 已知残余风险：OpenRouter单一聚合商代理，大客户迁移会造成份额噪声（见方法论文档，待二源交叉）
  AI_MODEL_USAGE_LOOSE_PCT: 3,     // 模型调用量趋势 >+3%（需求旺）→ loose
  AI_MODEL_USAGE_DECLINE_THRESHOLD_PCT: -3, // <-3%（需求降，供过于求预警）→ tight
  AI_CAPEX_YOY_LOOSE_PCT: 10,      // 云厂商capex同比 >+10%（扩产投资旺）→ loose
  AI_CAPEX_YOY_TIGHT_PCT: 0,       // <0%（capex转负，投资收缩）→ tight
  AI_SEMI_IP_YOY_LOOSE_PCT: 5,     // 半导体产出同比 >+5% → loose
  AI_SEMI_IP_YOY_TIGHT_PCT: 0,     // 同比 <0% → tight
  AI_SEMI_IP_LOOKBACK_DAYS: 400,

  // 信号档位常量
  SIGNAL: {
    LOOSE: 'loose',
    NEUTRAL: 'neutral',
    TIGHT: 'tight',
  },

  // 最终信号档位常量（防守分级 2026-07-12 用户拍板）：
  // attack=四维全宽松；reduce=仅单维收紧（减仓观望）；defense=双维以上收紧或锁激活（全面防守）
  FINAL_SIGNAL: {
    ATTACK: 'attack',
    NEUTRAL: 'neutral',
    REDUCE: 'reduce',
    DEFENSE: 'defense',
  },
};
